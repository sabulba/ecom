rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all access by default — rules will be configured in Phase 2
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
